{
  "name": "db-benchmark",
  "version": "1.0.0",
  "description": "Benchmark: PG Relational vs MongoDB vs PG JSONB-only",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "mongodb": "^6.16.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
